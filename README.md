# Excel-Uploader-Backend-React
Spring Boot batch application to process Excel files from a configured directory, validate format, insert data into database, and move files to success or error folders.
